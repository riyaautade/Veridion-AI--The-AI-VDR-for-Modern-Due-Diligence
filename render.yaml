services:
  # ──────────────────────────────────────
  # FastAPI Backend (Docker-based web service)
  # ──────────────────────────────────────
  - type: web
    name: veridion-ai-backend
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: free
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: veridion-ai-postgres
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_ALGORITHM
        value: HS256
      - key: ACCESS_TOKEN_EXPIRE_MINUTES
        value: 60
      - key: QDRANT_HOST
        value: PASTE_YOUR_QDRANT_CLOUD_HOST_HERE
      - key: QDRANT_PORT
        value: 6333
      - key: QDRANT_API_KEY
        sync: false  # Set this manually in Render dashboard
      - key: DOCUMENT_STORAGE_DIR
        value: /storage/documents
      - key: LLM_PROVIDER
        value: groq
      - key: GROQ_API_KEY
        sync: false  # Set this manually in Render dashboard
      - key: ALLOWED_ORIGINS
        value: https://veridion-ai-frontend.onrender.com  # Update after deploying frontend

  # ──────────────────────────────────────
  # React / Vite Frontend (static site)
  # ──────────────────────────────────────
  - type: web
    name: veridion-ai-frontend
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./frontend/dist
    rootDir: ./frontend
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        value: https://veridion-ai-backend.onrender.com/api  # Update with your backend URL

# ──────────────────────────────────────
# PostgreSQL Managed Database
# ──────────────────────────────────────
databases:
  - name: veridion-ai-postgres
    plan: free
